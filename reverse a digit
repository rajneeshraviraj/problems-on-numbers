#include<bits/stdc++.h>
using namespace std;
int rev(int n){
    int rev=0;
    int ld;
    while(n!=0){
    ld=n%10;
    rev=rev*10+ld;
    n=n/10;
    }
    return rev;
}
int main(){
    int n=123;
    cout<<rev(n);
    return 0;
}
